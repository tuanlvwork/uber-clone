---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: location-service
  namespace: uber-clone
spec:
  replicas: 1
  selector:
    matchLabels:
      app: location-service
  template:
    metadata:
      labels:
        app: location-service
    spec:
      initContainers:
        - name: wait-for-kafka
          image: busybox:1.35
          command: ['sh', '-c', 'until nc -z kafka 29092; do echo waiting for kafka; sleep 2; done;']
      containers:
        - name: location-service
          image: uber-clone:latest
          imagePullPolicy: IfNotPresent
          command: ['python', 'services/location_service.py']
          ports:
            - containerPort: 8005
              name: http
          envFrom:
            - configMapRef:
                name: uber-config
          livenessProbe:
            tcpSocket:
              port: 8005
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            tcpSocket:
              port: 8005
            initialDelaySeconds: 10
            periodSeconds: 5
          resources:
            requests:
              cpu: "50m"
              memory: "128Mi"
            limits:
              cpu: "200m"
              memory: "256Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: location-service
  namespace: uber-clone
spec:
  type: ClusterIP
  ports:
    - port: 8005
      targetPort: 8005
      name: http
  selector:
    app: location-service
