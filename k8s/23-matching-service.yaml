---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: matching-service
  namespace: uber-clone
spec:
  replicas: 1
  selector:
    matchLabels:
      app: matching-service
  template:
    metadata:
      labels:
        app: matching-service
    spec:
      initContainers:
        - name: wait-for-kafka
          image: busybox:1.35
          command: ['sh', '-c', 'until nc -z kafka 29092; do echo waiting for kafka; sleep 2; done;']
      containers:
        - name: matching-service
          image: uber-clone:latest
          imagePullPolicy: IfNotPresent
          command: ['python', 'services/matching_service.py']
          ports:
            - containerPort: 8004
              name: http
          envFrom:
            - configMapRef:
                name: uber-config
          readinessProbe:
            tcpSocket:
              port: 8004
            initialDelaySeconds: 10
            periodSeconds: 5
          livenessProbe:
            tcpSocket:
              port: 8004
            initialDelaySeconds: 30
            periodSeconds: 10
          resources:
            requests:
              cpu: "50m"
              memory: "128Mi"
            limits:
              cpu: "200m"
              memory: "256Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: matching-service
  namespace: uber-clone
spec:
  type: ClusterIP
  ports:
    - port: 8004
      targetPort: 8004
      name: http
  selector:
    app: matching-service
